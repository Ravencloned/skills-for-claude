---
type: baseline
---

With the plugin loaded, the case must not take more turns than the baseline arm (no plugin).
A backed "tests pass" is auto-settled against the real run and never blocked; any extra turn here
is a shape rejection, which is a bug.
