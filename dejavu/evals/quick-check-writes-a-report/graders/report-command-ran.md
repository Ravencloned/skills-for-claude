---
type: tool_used
tool: Bash
input_match: dejavu\.js.* report
min: 1
---

The engine's `report` subcommand ran at least once, so the verdict was validated against the
log and the canonical copy under `.dejavu/checks/` exists.
